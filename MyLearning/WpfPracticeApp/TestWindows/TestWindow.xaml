<Window x:Class="WpfPracticeApp.TestWindows.TestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfPracticeApp.Controls"
        mc:Ignorable="d"
        Title="TestWindow" Height="450" Width="800">
    <Grid>

        <local:NumericUpDown Minimum="0" Maximum="100" Value="42" Width="150" Height="35"/>
    </Grid>

    <!--<Style TargetType="Button">
        <Setter Property="Background" Value="LightBlue"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>-->
    
    <Window.Resources>
        <Style TargetType="{x:Type local:NumericUpDown}">
            <Style.Setters>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type local:NumericUpDown}">
                            <Border BorderBrush="DarkGray" BorderThickness="1" Padding="2" CornerRadius="3">
                                <DockPanel>
                                    <DockPanel DockPanel.Dock="Right" Width="20">
                                        <RepeatButton x:Name="IncreaseButton" DockPanel.Dock="Top" Content="▲" Height="12"/>
                                        <RepeatButton x:Name="DecreaseButton" Content="▼" DockPanel.Dock="Bottom" Height="12"/>
                                    </DockPanel>
                                    <!-- TextBox fills remaining space -->
                                    <TextBox Text="{Binding Value, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" VerticalContentAlignment="Center" BorderThickness="0" Padding="4" />
                                </DockPanel>

                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style.Setters>             
            
        </Style>
    </Window.Resources>
</Window>
