namespace DsaPractice.Arrays
{
    internal class SubStringPresent
    {
        public bool IsSubStringPresent(string s, string subString)
        {
            int subStringPointer = 0;

            foreach (var c in s)
            {
            }
            return false;
        }

        public void IsSubStringPresent()
        {
            string s = "abababc";
            string subString = "ababc";

            // rararam
            // raram

            // 

            Console.WriteLine(IsSubStringPresent(s, subString));
        }
    }
}
